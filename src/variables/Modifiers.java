package variables;

public class Modifiers {

    // A static integer, usable from any class
    static int staticInt = 10000;

    // A final integer, unchangeable after first initialization
    final int finalInt = 20000;

    // Volatile integer, not needed for this course
    volatile int volatileInt;

    Modifiers() {
    }
}
